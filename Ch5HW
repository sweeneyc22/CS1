/*
import java.util.Scanner;

public class SubtractionQuizLoop {
	private static int totalCorrect = 0;
	private static int totalWrong = 0;

	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		System.out.println("Initiated");

		generateAdditionQuestion(input, totalCorrect, totalWrong);

		System.out.println("Terminated");
		input.close();
	}

	private static void generateAdditionQuestion(Scanner input, int totalCorrect, int totalWrong) {
		int number1 = (int) (Math.random() * 15);
		int number2 = (int) (Math.random() * 15);
		System.out.println("What is " + number1 + " plus " + number2 + "?");
		int decision = input.nextInt();
		if (decision == (number1 + number2)) {
			System.out.println("Correct!");
			totalCorrect++;
			System.out.println("Total Correct: " + totalCorrect + " " + "Total Wrong: " + totalWrong);
			generateAdditionQuestion(input, totalCorrect, totalWrong);
		} else {
			System.out.println("Wrong!");
			totalWrong++;
			System.out.println("Total Correct: " + totalCorrect + " " + "Total Wrong: " + totalWrong);
			generateAdditionQuestion(input, totalCorrect, totalWrong);
		}
	}
}
*/

/*

import java.util.Scanner;

public class SubtractionQuizLoop {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);

		int computerWins,					// computer Wins
			 userWins;						// users Wins
		computerWins = userWins = 0; 	// Set totals

		do {
			// Generate a random integer 0, 1, or 2
			int computer = (int)(Math.random() * 3);
	
			// Prompt the user for choice
			System.out.print("Enter 0 for Scissors, 1 for Rock, and 2 for Paper");
			int user = input.nextInt();
	
			System.out.print("The computer is ");
			switch (computer)
			{
				case 0: System.out.print("scissor."); break;
				case 1: System.out.print("rock."); break;
				case 2: System.out.print("paper."); break;
			}
	
			System.out.print(" You are ");
			switch (user)
			{
				case 0: System.out.print("scissor"); break;
				case 1: System.out.print("rock"); break;
				case 2: System.out.print("paper"); break;
				default : System.out.println("Invalid entry");
					System.exit(1);
			}
	
			// Display result 
			if (computer == user)
				System.out.println(" too, It is a draw");
			else
			{
				boolean win = (user == 0 && computer == 2) ||
								  (user == 1 && computer == 0) ||
								  (user == 2 && computer == 1);
				if (win)
				{
					System.out.println(". You won");
					computerWins++; // Increment computer wins
				}
				else
				{
					System.out.println(". You lose");
					userWins++;		// Increment user wins
				}
				
			}
			// Print Score
			System.out.println("Computer wins: " + computerWins +
				"\nUser wins: " + userWins);  
		} while (Math.abs(computerWins - userWins) <= 2); //returns absolute value of compwins-userwins
		input.close();
	}
}
*/

/*
public class CoinFlipper {
	public static void main(String[] args) {
		int heads = 0, tails = 0;
		int coin;
		System.out.println("Flipping coin 1 million times...");
		for (int i = 0; i < 1000000; i++) {
			coin = (int) (Math.random() * 2) == 0 ? heads++ : tails++;
		}

		System.out.println("There were " + heads + " heads and " + tails + " tails.");
	}
}
*/
